{% extends '::base.html.twig' %}
{% import 'PlanillasCoreBundle:Macros:macros.html.twig' as macros %}
{% block title %}
    Modificar Cursos
{% endblock %}
{% block content_content %}
    {% for mensaje in app.session.flashbag.get('info') %}
		<div class="alert alert-info">{{ mensaje }}</div>
    {% endfor %}

    <div class="row">
        {% block content %}
            <div class="col-lg-9">
                <div class="panel panel-default">
                    <div class="panel-heading">
                            <h2 class="panel-title">Cursos</h2>
                    </div>        
                            <div class="panel-body">
                                <table class="table table-bordered table-hover table-responsive">
                                    <thead>
                                        <tr>
                                            <th>Nombre</th>
                                            <th>Descripción</th>
                                            <th>Fecha de Vencimiento</th>
                                            <th>Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                    {% for entity in entities %}
                                        <tr>
                                            <td><a href="{{ path('cursos_edit', { 'id': entity.id }) }}">{{ entity.nombre }}</a></td>
                                            <td><a href="{{ path('cursos_edit', { 'id': entity.id }) }}">{{ entity.descripcion }}</a></td>
                                            <td><a href="{{ path('cursos_edit', { 'id': entity.id }) }}">{% if entity.vence %}{{ entity.vence|date('Y-m-d') }}{% endif %}</a></td>
                                            <td>
                                                <div style="float: left">
                                                <a class="btn btn-primary" href="{{ path('cursos_edit', { 'id': entity.id }) }}">Editar</a>
                                                </div>     
                                                <div style="float: left; padding-left: 10px">
                                                {{ form_start(aDeleteForm[entity.id]) }}
                                                {{ form_errors(aDeleteForm[entity.id]) }}
                                                {{ form_end(aDeleteForm[entity.id]) }} 
                                                </div>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>

                                   
                                        <a class="btn btn-primary" href="{{ path('cursos_new', { 'id_empleado': eEmpleado.id }) }}">
                                            Nuevo
                                        </a>
                            </div>                    
                </div>
            </div>
            <div class="col-sm-3">
			    {{ macros.empleado_foto(eEmpleado) }}
                {{ macros.menu_empleado('cursos',eEmpleado) }}
            </div>
        {% endblock content %}    
    </div>
    {% endblock %}
