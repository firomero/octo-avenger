{% extends '::base.html.twig' %}
{% import 'PlanillasCoreBundle:Macros:macros.html.twig' as macros %}
{% block title %}
    Nuevo Idiomas
{% endblock %}
    
{% block content_content %}
   {% for mensaje in app.session.flashbag.get('info') %}
    <div class="alert alert-info">{{ mensaje }}</div>
   {% endfor %}
    
    <div class="row">
        {% block content %} 
            <div class="col-lg-9">
              <div class="panel panel-default">
                <div class="panel-heading"> <!-- Encabezado panel central -->
                    <h2 class="panel-title">Nuevo idioma</h2>
                </div>
                <div class="panel-body">
                    {{ form_start(form) }}
                     <fieldset>
                            
                            {{ form_errors(form) }}
                            <div class="row-fluid">
                                <div class="col-lg-4">{{ form_row(form.idioma) }}</div>
                                <div class="col-lg-4">{{ form_row(form.porientoIdioma) }}</div>
                            </div>
                        </fieldset>
					 <button type="submit" name="submit_button" class="btn btn-success">Crear</button>
					 <a href="{{ path('educacionidiomas', { 'id_empleado': entity.empleado.id } ) }}" class="btn btn-success">Regresar</a>
                    
                   {{ form_end(form) }}
				   
                </div>
              </div>
            </div>
			<div class="col-lg-3"> <!-- Menu derecho de empleado -->
			 {{ macros.empleado_foto() }}
             {{ macros.menu_empleado('educacionidiomas',entity.empleado) }}
            </div>
        {% endblock content %}     
    </div>
    
    
   
{% endblock %}
