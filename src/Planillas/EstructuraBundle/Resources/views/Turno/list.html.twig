<table class="table table-striped table-bordered table-condensed table-responsive">
    <thead>
    <tr>
        <th>Nombre Turno</th>
        <th>Empresa</th>
        <th>Cliente</th>
        <th>Sucursal</th>
        <th>Acciones</th>
    </tr>
    </thead>
    <tbody>
    {% for e in entities %}
        <tr>
            <td>{{ e.nombre }}</td>
            <td>{{ e.empresa }}</td>
            <td>{{ e.cliente }}</td>
            <td>{{ e.sucursal }}</td>
            <td></td>
        </tr>
    {% else %}
        <tr><td colspan="3">No se encontraron elementos.</td></tr>
    {% endfor %}
    </tbody>
</table>

<div style="text-align: center;">{{ knp_pagination_render(entities) }}</div>

