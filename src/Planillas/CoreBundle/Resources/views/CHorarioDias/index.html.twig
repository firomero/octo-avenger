{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CHorarioDias list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dia</th>
                <th>Horainicio</th>
                <th>Horafin</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('chorariodias_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.dia }}</td>
                <td>{{ entity.horaInicio }}</td>
                <td>{{ entity.horaFin }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('chorariodias_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('chorariodias_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('chorariodias_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
